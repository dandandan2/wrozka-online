# Copy to .env.test to override the dummy defaults baked into
# tests/setup/astro-env-server.ts. Not required to run the suite — every
# test in this rollout phase is hermetic (mocked Supabase client/Auth) and
# works with the built-in dummy values.
#
# Only useful once a Docker-capable environment adds real integration tests
# against a local `supabase start` instance (see plan.md's Open Risks):
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_KEY=###
OPENROUTER_API_KEY=###
